---
type: journal
created: 2026-01-29
updated: 2026-05-27
tags: [zbz-ocr-tei, journal]
status: active
---

# Arbeitsjournal

Kompakter chronologischer Ueberblick aller Arbeitssitzungen. Eine Zeile pro Sitzung,
Details und Entscheidungen in [decisions.md](decisions.md). Code-Aenderungen im Git-Log.

---

## Juni 2026 — Abnahme-Vorbereitung

| # | Datum | Thema |
|---|---|---|
| 63 | 2026-06-07 | Viewer-Kuration: **ein** Speichern-Knopf (E78) + Mirror-Write-Fix (E79). Ausloeser: User korrigierte Layout auf doc 30/Seite 3, "Speichern" -> Reload -> Edit weg. Ursache zweischichtig: der server-lose Viewer laeuft mit **Docroot=`docs/`** und liest beim Reload nur aus `docs/data/` (`output/` von dort unerreichbar), waehrend [[E72]] die Kuration nur nach `output/` schrieb; zusaetzlich las `fetchLayout` nie `_layout_curated.json` (anders als die Pipeline `loaders.load_layout_gemini`). Fix (E79): jeder FSA-Write spiegelt die identische Nutzlast zusaetzlich in den Mirror (`pages/` + `manifests/`), Viewer liest curated zuerst (`layoutCurated > gemini > docling`); kanonischer `output/`-Schrieb unveraendert, `--mirror-only` reproduziert dieselben Dateien (kein Drift). Bereits gespeicherte Seite gezielt in den Mirror kopiert (kein 285-Doc-Lauf). Verifiziert: beide Stellen byte-identisch (md5), `node --check` + HTTP-200, Round-Trip dicht. Davor in derselben Welle (E78, retrospektiv nummeriert): drei Einzel-Download-Buttons zu **einem** "Speichern" zusammengefuehrt (alle Stroeme + Manifest an kanonische Repo-Stelle), Einzel-Downloads ins **"Export ▾"**-Dropdown, Identity-Chip neben Speichern, Seitennav in den Faksimile-Panel-Header, Toggles "Layout/Text bearbeiten", "Ordner verbinden" nur noch ueber Speichern (+ Erst-Info-Modal + `looksLikeRepoRoot`-Check). Doku-Sync E78/E79 (decisions.md, viewer.md §Persistenz, workflow.md §3, CLAUDE.md/index.md/README). Kein Commit (gemischter Tree, mehrere Instanzen). Lehre: eine server-lose Persistenz hat **zwei** Wahrheiten -- den kanonischen Konsum-Ort (`output/`, Pipeline) und den Lese-Ort des Frontends (`docs/data/`-Mirror); wer nur in den ersten schreibt, speichert real, aber unsichtbar fuer den, der gerade kuratiert. |
| 62 | 2026-06-07 | Workflow-Status von vier auf drei Stufen kollabiert (E77, Variante A). Ausloeser: Frontend gestartet (`http.server` auf docs/, Port 8137) und Status-Pills im Viewer angeschaut -- die vier Stufen `unverifiziert/in_arbeit/bearbeitet/fertig` kollabierten im UI ohnehin auf nur zwei Ampel-Farben, `bearbeitet` vs `fertig` eine unscharfe Naht. Entscheidung (nach Vorlage zweier Farbschemata): drei Stufen `unverifiziert\|in_arbeit\|verifiziert`, **eine Farbe je Stufe** (grau/gelb/gruen), **rot reserviert** (E67-konform, literale rot/gelb/gruen-Ampel verworfen). Umgesetzt ueber Backend (page_manifest `VALID_STATUS`/`STATUS_MIGRATION`, tei_status_marker Labels, generate_edition_data Mirror-Migration), Frontend (viewer.js/catalog.js `STATUS_CYCLE`/`LABEL`/`LEGACY` + Tooltips, index.html Filter 4->3), CSS (Pill-/Ampel-Dots auf `--h-text-muted`/`--h-ocker`/`--h-olivgruen`). **Keine Mirror-Regeneration noetig**: alle 285 Docs standen auf `unverifiziert`, History leer -> bestehende catalog.json/Manifeste schon konform (per Grep + Histogramm verifiziert). Neues Gate `tests/test_workflow_status.py` (5 Tests, inkl. Legacy-Migration + History-`from`/`to`). Suite **520->525 gruen**. Doku nachgezogen (E77 in decisions.md, quality.md §Workflow-Status, CLAUDE.md, index.md). Lehre: vier Zustaende in zwei Farben sind Informationsverlust in der Ampel; drei Zustaende mit je eigener Farbe sind ehrlicher -- und der ideale Umstellungszeitpunkt ist, solange noch kein Doc den wegfallenden Status traegt. |
| 61 | 2026-06-03 | Abnahme-Tiefenanalyse (User-Auftrag) + Repo-Hygiene + MMSID-Entfernung (E76). Gegen die Platte statt Prosa verifiziert: **524 Tests gruen**, Korpus **325->289->286->285** (0 Drift, `corpus_audit`), **285/285 tei_final schema-valide**, revisionDesc sauber (0 Agent-Screening-Reste, 855 Workflow-Status-Eintraege, 15 Blank-Marker). Befunde fuer die Abnahme: **855/855 Stroeme `unverifiziert`** (M5 fachliche Kuration offen), NER aus Scope entfernt ([[E71]], ZBZ-Zustimmung noetig), Container/CI-CD pending, Faksimile online nur 4 Demo-Docs (Rest gitignored), **MMSID im Code aber 0/285 ausgeliefert**, **195/285 leere Container-Titel** (`<monogr><title/>`, Daten nur als Masterfile-Freitext/Alma), Doc 10 unvollstaendig (285/286), Sprachfilter-Fragmentierung im Catalog. Hygiene: 3 gemergte Branches + `data/schema/tei_all.rng.bak` geloescht (output/_backup_* gitignored, belassen). **MMSID-Entfernung (E76):** vor dem Loeschen den Spec-Konflikt vorgelegt — die Editionsrichtlinien (E49) fordern ID+MMSID+PubForm im Header — User entschied dennoch fuer Entfernen (Header-Metadaten = ZBZ-Domaene, O8); `scripts/core/masterfile.py` + Verdrahtung in `tei_unified`/`tei_step3` + Test raus, ZBZ-Richtlinien-Datei (Input) unberuehrt, Suite 520 gruen. Zusaetzlich Root-`README.md` abnahmetauglich neu gefasst (EN, User-Entscheidung): Status-Sektion (Pipeline fertig + schema-valide vs. Edition `unverified`/M5), Scope-&-Limitations (NER E71 / Container+CI-CD pending / Header-Metadaten = ZBZ-Domaene), CER-Headline (Fidelity 1.83%/4.26%), Drift-Fixes (285/286, E1-E76, JSZip als planned, facsimile/zone bereits vorhanden, Demo-Bild-Hinweis). Lehre: vor einer Spec-konflikt-Loeschung die Anforderung sichtbar machen und die Entscheidung dem Lead lassen — nicht stillschweigend ausfuehren, aber auch nicht verweigern. |

---

## Mai 2026 — Viewer-Datenversorgung + Deploy-Vorbereitung + Edition-Uplift

| # | Datum | Thema |
|---|---|---|
| 60 | 2026-05-27 | Frontend-UI-Review aller 5 `docs/`-Seiten im Browser (Catalog/Methode/About/Impressum/Viewer inkl. Layout-/Text-Editor + Responsive) + Quick-Win-Umsetzung (User-Auftrag, kontrolliert beendet). **Kernfund Viewer:** `getAuthor()` oeffnete ein blockierendes `window.prompt()` fuers Bearbeiter-Kuerzel, ausgeloest als Seiteneffekt beim ERSTEN Klick auf Layout/Text/Status-Pill (ueber `autoStartArbeit`) -- fror Renderer UND Automation hart ein. Ersetzt durch Inline-Kuerzel-Feld in der Status-Subbar (localStorage, kein prompt mehr). **#2:** `autoStartArbeit` vom blossen Oeffnen entkoppelt -> `unverifiziert->in_arbeit` erst bei der ERSTEN echten Aenderung (`onLayoutChanged` / transcription-`onChange`), nicht beim Reinschauen (vermied ungewollten Statuswechsel + beforeunload-Warnung). **#13:** Dirty-Marker `*` jetzt pro Strom (`state.dirtyStreams`) statt manifest-weit auf allen drei Pills. **#3:** Mobile -- Subbar-Titel zerfiel <600px in abgeschnittene Wort-Fragmente; Media-Query (volle Zeile + nowrap/Ellipsis). **#9:** XML-Ansicht `white-space:pre-wrap` statt horizontalem Abschnitt. **Catalog #4:** Strom-Filter ohne Status war wirkungslos, schrieb aber `?stream=` -> bis Status gewaehlt deaktiviert. **#10:** Treffer-Zaehler ("X von 285"). **#12:** Datums-Sortierung schiebt die 88 datumslosen Docs ans Ende. **Doku #5:** methode.html Limitations `n=19`->`n=25` (Widerspruch zur E73-Methodik). **#6:** impressum.html `§ 5 TMG` (DE Recht) -> CH/AT-Formulierung. Latenter Deploy-Mangel mitbehoben: JS-Includes hatten kein `?v=` (nie cache-invalidiert) -> alle docs-Skripte + geaenderte CSS versioniert. Alles im Browser verifiziert (kein Freeze, Pills clean, Zaehler/Filter/Sort live), Konsole fehlerfrei, Bild-Abdeckung korpusweit (287 Ordner). Bewusst NICHT angefasst (Design-Entscheidung noetig): toter Panel-Divider (`ew-resize`-Cursor ohne JS-Handler), TEI-gerendert-Edit ohne Speicherpfad (`downloadTei` verwirft Edits ausser im XML-Modus), Umlaut-Transliteration der UI-Chrome (`fuer/Qualitaet` trotz sauberer franz. Diakritika), fragmentierte Sprachfilter-Werte (`DE` vs `DEU`, Reihenfolge-Varianten). Lehre: ein `prompt()` als Seiteneffekt eines Edit-Toggles blockiert den Renderer hart, nicht nur haesslich -- und ein "in Arbeit"-Status, der beim blossen Hinschauen kippt, verwandelt Inspektion in unbeabsichtigte Mutation. |
| 59 | 2026-05-27 | Repository-Aufraeum-Welle (Welle 1-5, 10 Commits auf `cleanup/repo-aufraeumen`, noch nicht gemergt). **W1-3:** Doku-Drift (NER-Reste, GitHub-Pfade, CER/Korpus-Zahlen auf SSoT), tote NER-Reste + `--compare-ref`-Bugfix (`ref_body` nur im except-Zweig definiert -> UnboundLocalError), Design-Tokens Hex->`--h-*`. **W4 (R3, getestet):** OCR-Quellen vereinheitlicht (`loaders.OCR_SOURCES` als einzige Quelle, kuratiertes OCR fliesst jetzt ins PAGE-XML); Discovery konsolidiert (`extract_page_num`->utils; layout-scoped Discovery nach loaders -- **Fund: page_xml `discover_pages/documents` waren KEINE Duplikate, sondern Layout- vs OCR-Scope -> Namensfalle, nicht blind zusammengefuehrt**); Padding-Konvention in `utils.page_{md,image,layout}_name` zentralisiert. byte-identisch verifiziert (Doc 2310 PAGE-XML). **W5 (4 User-Entscheidungen):** W5.1 -- auf User-Wunsch CER-Scope an den Rohdaten geprueft: die hartkodierte 6er-`SCOPE_MISMATCH_REASONS` war **inkohaerent** (schloss Doc 570 mit 112% Mehrtext/Voll-CER 113% NICHT aus, markierte aber 3020/760/830 mit ~1%; zwei Kommentare faktisch falsch), der "Volltext-CER 22.88% n=19" war scope-artefakt-dominiert. User: "kaputte Zahl raus" -> Liste + `_override_scope`-Heuristik entfernt, alle Metriken n=25, **Fidelity 4.26%/1.83% bleibt exakt** (E73). W5.2 Schematron: Spike bewies die 36 eingebetteten Regeln sind generisches TEI-ODD-Boilerplate (14 brauchen XPath 2.0, die 22 lauffaehigen 0 Treffer ueber 285) -> dokumentiert statt gebaut (E74). W5.3/W5.4: `ocr_dedup` (verwaist+destruktiv) + DoclingOCR-Engine (Sackgasse+Fehlrouting) entfernt (E75). Suite 516->524 gruen. .md-Konsistenz nachgezogen (methode/quality/pipeline/Arbeitsbericht/decisions E73-E75). Lehre: ein Backlog aus einer Analyse ist eine Hypothese, kein Auftrag -- gegen die Platte geprueft entpuppten sich zwei "Duplikate" als bewusst verschiedene Scopes und eine "fair bereinigte" Headline-Zahl als willkuerlich; die ehrlichste Antwort auf eine kaputte Metrik ist, sie zu entfernen, nicht sie zu repariert-verschoenern. |
| 58 | 2026-05-27 | Direkt-Schreiben-Loop fuer die Viewer-Kuration (E72; Nutzer-Auftrag: "manuell ins Repo zurueckzulegen ist keine gute Loesung"). Befund vorab: der Browser kann aus Sandbox-Gruenden nicht still auf die Platte schreiben (kein Defekt, sondern das Vertrauensmodell), UND die kuratierten Layout-/OCR-Dateien hatten gar keinen Pipeline-Konsumenten -- nur das Manifest war verdrahtet, der in viewer.md dokumentierte Round-Trip also teils aspirativ. Zwei Haelften gebaut: (a) Frontend `docs/assets/js/fs-access.js` (`ZBZ.FsAccess`) schreibt per File System Access API direkt in den einmal freigegebenen Repo-Ordner (Handle in IndexedDB, Schreibrecht pro Sitzung per Geste); `viewer.js` Save-Handler via `persist()` FSA-mit-Download-Fallback, neuer "Ordner verbinden"-Button. (b) Backend `loaders.py`: `load_layout_gemini` liest `_layout_curated.json` zuerst, `OCR_CURATED_DIR` (`output/ocr_curated/`) als erstes `_OCR_DIRS`-Element -> `--reassemble` nutzt die Edits real. Regeneration per dokumentiertem One-Liner. Gate `tests/test_curated_loaders.py` (9 gruen). Chromium-only (Fallback Download fuer Firefox/Safari). Out of scope (Nutzer): lokaler Watcher + GitHub Actions. Nummer E70 war im Journal bereits von der CER-Parallel-Instanz belegt, daher E72. Lehre: "direkt zurueckschreiben" ist zwei Probleme -- der Schreibweg (FSA) UND ein realer Konsument; eine dokumentierte Persistenz ohne Loader-Konsumenten ist eine Round-Trip-Fassade. |
| 57 | 2026-05-27 | Doku-Korrektheits-Welle (parallel zu E70/E71 zweier anderer Instanzen). Alle Markdown-Docs (README, CLAUDE.md, knowledge-Docs, scripts-README) gegen den realen Repo-Stand auditiert (Skript-Baum, OCR-Engines im Code, Test-Dateien, Schema-Pfad, `tei_final`-Zaehlung). Gefixt: veraltete Entscheidungs-Zaehler (`E1-E61`/`E1-E65`/`E1-E68` -> aktuell); `workflow.md` von der mit E66 abgeschafften Agent-Screening-Welt befreit (Diagramm + Datenformat-Tabelle + §4 Provenance + §7 Drift-Sektion auf Workflow-Status-pro-Strom umgestellt, `review.json` als Legacy markiert); neue Artefakte nachgetragen (opt-in Gemini-Vision-OCR `-e gemini`, `scripts/core/masterfile.py` + `tei/pb_split.py`, Test-Inventar 8 Dateien inkl. `22->24`-Korrektur); `projekt.md` Viewer-Uplift-Status (E58-E67 ausgeliefert statt "geplant"); README knowledge-Doc-Zahl + `3 Stufen`-Konsistenz. Waehrend der Arbeit landeten E70 (CER) + E71 (NER-Entfernung) der Parallel-Instanzen; daraufhin die eigenen `E1-E69` auf `E1-E71` nachgezogen und `README.md` als einzige vom NER-Sweep noch nicht erreichte Datei E71-bereinigt (toter `entities.md`-Link, geloeschtes `scripts/ner/`+`data/entities/`, NER-Zeilen raus). Bewusst NICHT angefasst: D-Zone (CER-Headline, Validator-Regelzahlen, Schema-Definitionen = E70-Territorium) und die von der E71-Instanz gehaltenen Dateien (`pipeline.md`/`quality.md`-Prosa, `journal.md`-Altlink in #?). Kein Commit (Working Tree mischt drei Instanzen). Lehre: bei mehreren zeitgleichen Agenten sind `git status` + Verifikation gegen die Platte Pflicht, und man editiert nur, was kein anderer Agent gerade haelt -- ein "file modified since read"-Bounce ist das Signal zum Zuruecktreten, nicht zum Erzwingen. |
| 56 | 2026-05-27 | NER/Entity-Linking vollstaendig aus der Pipeline entfernt (E71, User-Entscheidung). Befund am ausgelieferten TEI: von ~30.500 getaggten Erwaehnungen trugen nur ~2.6% eine echte GND-ID, 1.274x `GND:unknown`, Rest interne `#zbz-`-IDs; das dokumentierte Dual-Attribut [[E50]] existierte im Output gar nicht (0x `corresp="#zbz-"`) -- die Verlinkung, der eigentliche editorische Mehrwert, war nie lieferfaehig. Code entkoppelt: `--ner` aus `tei_unified`, Entity-Seeding + Mapping-Table-Section-4 aus `tei_mapping_prompt`, `annotate_entities`/`reannotate_entities` aus `tei_step1/2`+`tei_generator`, NER-Konstanten aus `config.py`, `export_entity_index` aus `generate_edition_data`. Geloescht: `scripts/ner/` (6 Module), `data/entities/*.xml`, `docs/data/entity_index.json`, `output/{ner_report.html,entities,tei_ner}`. Frontend: Entity-Highlighting aus `tei-render.js` + `.tei__entity*` aus `viewer.css` + NER-Bullet aus `about.html`. Deterministischer Regex-Body-Strip (formatierungs-/CRLF-treu, nur Tags fallen weg) ueber alle 285 TEI in drei Ablagen: `<persName>`/`<orgName>`/`<placeName>` + Inline-Werk-`<bibl>` aufgeloest, `<bibl>` in `<listBibl>` + Review-`<head>` als bibliografische Struktur behalten (ref/corresp raus). 285/285 schema-valide (`test_tei_schema` 287 gruen), Script-Health 90 gruen. Doku: entities.md geloescht, CLAUDE.md/index.md/pipeline.md/scripts-README auf 6-Stufen-Pipeline + 10 Knowledge-Docs nachgezogen, R3/R10 hinfaellig. Restposten: stale Entity-Prosa in 7 `<revisionDesc>`-`<change>` (Agent-Screening-Log im teiHeader). Lehre: ein Feature, dessen Output zu ~97% aus Platzhaltern (`GND:unknown`/interne IDs) besteht, ist kein halbfertiges Feature, sondern Rauschen, das Verlaesslichkeit vortaeuscht -- Entfernen ist ehrlicher als Behalten. |
| 55 | 2026-05-27 | CER-Methodik tief geprueft + korrigiert (E70), auf User-Auftrag "ganz genau und richtig", extern verifiziert (OCR-D/dinglehopper/Transkribus/jiwer/Singh 2025). Kernbefund per konkretem Beispiel-Dump: die ZBZ-Referenzen sind selektive **Teiltranskriptionen** (Doc 580 = 2 Rezensionen vs 1, Doc 570 = Masthead fehlt, Doc 90 = Inhaltsverzeichnis), die Pipeline ist oft vollstaendiger. Das alte `find_best_alignment`-Trimming verbarg Insertions+Verluste (+ Padding-auf-Referenzlaenge = Deflation); naive Volltext-CER bestraft umgekehrt Vollstaendigkeit (Doc 570: 113%, OCR fehlerfrei). Loesung: `classify_edit_operations` zerlegt jede Editieroperation in **Fidelity** (Subst.+kleine Indels+ALLE Loeschungen = echte Fehler) vs **Scope** (grosse Einfuegungen >=50 = Pipeline-Mehrtext), additiv `cer_fidelity+scope=cer`. Headline=Fidelity **Mean 4.26%/Median 1.83% ueber ALLE 25 Docs** (kein zirkulaerer Ausschluss noetig, reproduziert die alte Zahl sauber); nach Transkribus-Baendern "exzellent/gut". Weitere Fixes: pauschales `.lower()` raus (case-sensitiv Default, Effekt ~0); drei CER-Pfade (`benchmark_cer`/`cer_statistics_full`/`tei_validator --compare-ref`) auf `extract_text_for_comparison`+`calculate_cer` vereinheitlicht (`--compare-ref` nutzt `tei_final/` statt rohem `itertext()`); zirkulaeres `cer>50%`-Ausschlusskriterium raus + latenten Feldbug `ref_pages_total`->`ref_pages` behoben (Page-Ratio-Filter feuerte nie); Blank-`<pb>` aus Page-Count; Fehlerkategorien via `rapidfuzz.opcodes` statt `difflib` (summieren exakt zur Distanz); Paired-Test like-for-like auf Fidelity = **-7.12pp p=0.14 (n=19, nicht signifikant)**, frueheres "-14.83pp p=0.0004" als Trimming-Artefakt zurueckgezogen; Zitation Levchenko 2025 (nicht "Nosova et al."). 18 neue goldene Tests `tests/test_cer_extraction.py`, Suite **507 gruen**, `docs/data/cer_statistics.json` neu (Seed 42). OFFEN (User-Entscheidung): ist Topf A (Pipeline-Mehrtext) erwuenschter Editions-Inhalt oder rauszufilternder Beifang? Lehre: eine "zu niedrige" Metrik kann genauso ein Artefakt sein wie eine zu hohe -- hier verbarg Trimming die Wahrheit, und die Wahrheit war, dass die Referenz unvollstaendig ist, nicht die OCR schlecht. |
| 54 | 2026-05-27 | Hygiene + Korrektheits-Welle (E69). Welle 0: uncommittete `docs/`-Asset-Reorg (`docs/css|js` -> `docs/assets/...`, kohaerent, HTML-Pfade nachgezogen) committet; Branch `refactor/scripts-structure` (9 Commits, inkl. scripts/-Reorg + E68-Schema-Fix + corpus_audit) per Fast-Forward nach `main` gemergt und gepusht. Welle 1 auf `fix/correctness-wave`: drei stille Korrektheitsprobleme, je mit Test-Gate. **A3/O24**: `tei_validator._compute_cer` importierte ein nicht existentes `compute_cer` -> Import schlug seit jeher still fehl (`except (ImportError, Exception)` schluckte alles), Validator fiel auf grobe Laengen-Approximation; Fix `calculate_cer` * 100 (Ratio 0-1 -> Prozent, passend zur Report-Formatierung `{cer:.1f}%`), `except` auf `ImportError` verengt, 5 Tests. **A4/`<pb>`-DRY**: die Regel "Seitenzahl = 1-basierte sequenzielle pb-Position" + Regex + Body-Extraktion war in `generate_edition_data.py` UND `tei_blank_marker.py` dupliziert (nur per Kommentar synchron, Drift = stiller Off-by-Page-Bug); gemeinsamer Helfer `scripts/tei/pb_split.py` (`PB_RE`, `BODY_INNER_RE`, `iter_page_spans`->`PageSpan`), beide Konsumenten umgestellt, **byte-identisch verifiziert** ueber alle 285 Finals (Segmentierung) + 15 Blank-Reports via Baseline-Snapshot vor/nach; `tei_status_marker` ist NICHT betroffen (nur revisionDesc); 9 Tests. **A1/Header-Haertung**: `build_tei_header` war aermer als das ausgelieferte `tei_final` (docID nur als Kommentar, einfaches `<bibl>`, kein `langUsage`) -> jeder `tei_unified`-Neulauf haette `<idno>`/`biblStruct` regressiert (die E68 erst schema-valide machte); Neufassung deckungsgleich zum Liefer-Vertrag (`<idno type="docID">` + `<biblStruct type=pub_form>`/`<analytic>`/`<monogr>`/`<imprint>` + `<langUsage>` je Sprachcode via `_language_idents`); revisionDesc bleibt `tei_add_revision`+`tei_status_marker`. **MMSID/O8 (teilweise)**: `scripts/core/masterfile.py` liest die Alma-MMSID aus der Masterfile (Spalte MMSID, 216/325 Texte), Header fuehrt sie konditional als `<idno type="MMSID">`; verdrahtet in `tei_unified`. Verifiziert: Header valide gegen `zbz_hersch.rng` in allen Kanten, `--reassemble doc 100` end-to-end VALID mit reichem Header inkl. MMSID; 19 Tests. Suite 466 -> **503 gruen**. O24 geschlossen, O8 fortgeschritten. `fix/correctness-wave` noch nicht nach `main` gemergt (User-Review). Lehre: ein `except (ImportError, Exception)` verschluckt jeden Bug; eine "nur per Kommentar synchron"-Duplikation braucht einen Baseline-Beweis beim Entkoppeln; und ein Generator, der aermer ist als sein eigener Liefer-Stand, ist eine Zeitbombe, die erst beim naechsten Neulauf zuendet. |
| 53 | 2026-05-27 | Schema-Regression entdeckt und behoben (E68). Auf die Frage, ob gegen DTA-Basisformat validiert wird, ergab die Quellpruefung: der Validator prueft gegen das projekteigene `zbz_hersch.rng` (aus ODD, E48), nicht direkt DTA — und die ausgelieferte Schicht wird gar nicht batch-validiert (`validate_all` erwartet verschachteltes `tei_unified`, `tei_final` ist flach abgelegt; `tei_blank_marker`/`tei_status_marker` schreiben ohne Re-Validierung). Erste korpusweite Validierung von `tei_final`: **0/285 valide** — alle Fehler im teiHeader (`revisionDesc`/`change`, `langUsage`, `idno`, `biblStruct/monogr`), die das ODD-Subset weggelassen hatte; gegen das alte `tei_all.rng` validieren dieselben Docs 8/8. Damit waren zwei Prosa-Aussagen faktisch falsch: quality.md "285/285 valide (nach Schema-Fix)" und der Fix-001-Schluss (2026-03-26) "idno/langUsage/biblStruct = reine Kaskaden-Artefakte" — die Elemente waren real verboten; als der Header spaeter (E65/E66) reicher wurde, fiel `tei_final` auf 0/285, unbemerkt mangels Batch-Validierung. Fix E68: 7 Definitionen + 4 Inhaltsmodelle in `zbz_hersch.rng` ergaenzt (Inhalt minimal am korpusweit erhobenen Datenvertrag), `source-metadata` als kuratierter div-Typ in `config.py` (Doc 1170). Ergebnis **285/285 valide**. Neues Gate `tests/test_tei_schema.py` (Schema kompiliert + synthetischer E68-Header valide + jedes `tei_final` valide; gegen die alte Schema-Version als rot verifiziert, also nicht-leer). Volle Suite **464 gruen**. O23 geschlossen, quality.md + decisions.md korrigiert. Branch `refactor/scripts-structure` (mit Vortags-Arbeit: scripts/-Reorg nach Domaenen, `corpus_audit.py`, `test_scripts_health.py`) noch nicht nach `main` gemergt. Lehre: eine Prosa-Zahl ("285/285 valide") ist kein Beleg — die ausgelieferte SSoT braucht ein Gate, keine Behauptung; und ein gruenes Validierungsergebnis taeuscht, wenn der Validator die falsche (verschachtelte) Ablage prueft statt der flachen Liefer-Schicht. |
| 52 | 2026-05-26 | E66-Abschluss: Workflow-Status in TEI projiziert + 4 Commits gepusht + Frontend-Audit + tote Screening-Badges entfernt. `tei_status_marker` scharf ueber alle 285 Docs gelaufen: 285 irrefuehrende Agent-Screening-`<change>`-Eintraege ("APPROVED") entfernt, 855 ehrliche Summary-Eintraege gesetzt (3 pro Doc: ocr/layout/tei = `unverifiziert`), Backup in `output/_backup_pre_status_marker/`. Mirror neu (285 Files, +855/-285 Z. in `docs/data/pages/`). Vier lokale Commits (`863480fc`, `a9f93eb9`, `5b08309f`, `c70e9cb4`) zu `origin/main` gepusht — E66/E67 jetzt fuer ZBZ sichtbar. Frontend-Audit ueber HTML (720 Z.), CSS (1670 Z.), JS (2230 Z.): 15 Befunde priorisiert, davon vier tote Selektoren (`.badge--screening/--approved/--with-notes/--needs-review`) in [viewer.css](../docs/css/viewer.css) Z. 64-78 entfernt (Agent-Screening-Relikte, [viewer.js](../docs/js/viewer.js):568 bestaetigt "abgeloest durch Workflow-Pills"). Offene Quick-Wins fuer naechste Sitzung: 7 Hex-Verstoesse in `base.css`/`viewer.css` (sollten `--h-gruen/orange/rot`-Tokens werden), Ampel-Dot-Duplikat zwischen `catalog.css` und `viewer.css`. Mittel: Header-Drift (5 HTML-Dateien, `site-header__center`-Badge nur in 2), `viewer.js` (715 Z.) modul-split in Manifest/Pages/Faksimile, Status-Cycle stoppt bei `fertig`, Provenance-Drawer. Lehre: Sitzung kontrolliert beenden = Diff + Push + Doku + Memory in einem Zug -- nicht "morgen mache ich noch...". |
| 51 | 2026-05-26 | Catalog-UI-Refactor + Ampel-Reframing + Site-Konsistenz (E67). Iterative UI-Welle auf der Korpus-Uebersicht in mehreren Runden: (1) Status-Bar entfernt (keine Pseudo-Prozess-Uebersicht mehr), (2) Filter-Bar in eine Reihe mit 32-px-Hoehe ueber alle Elemente (Search/Selects/Reset), (3) Sortierung umgestellt: Dropdown raus, Spaltenheader klickbar mit Affordance im Ruhezustand (`↕` gedimmt, aktiv rot `↑`/`↓`), Autor und Datum separate Spalten, (4) Tabellenkopf nicht mehr eigene beige Flaeche -- gleiches Surface wie Body, nur Trennlinie; eine Body-Schrift fuer alle Zellen, mono nur fuer Doc-ID + Stream-Labels, (5) Workflow-Spalte iteriert: erst 3 Zeilen mit Statustext (verbose), dann pure Dots (kryptisch), final vertikal mit Labels OCR/Layout/TEI-XML, (6) sticky Filter-Bar loest frueheren Doppel-Scroll (Tabelle hatte eigenen overflow). User-Befund kollabiert in zwei honest reframings: (a) "kein Mensch hat approved" → Agent-Screening hat keine Aussagekraft; (b) "Pipeline-Output existiert, ist nur unverifiziert" → `offen=rot` ist semantisch falsch. Beide gefixt: Status `offen` umbenannt in `unverifiziert`, idempotente Migration in `page_manifest.py` (v2->v3); Ampel-Mapping nachgezogen: gelb fuer `unverifiziert/in_arbeit/bearbeitet`, gruen fuer `fertig`, rot reserviert. Catalog regeneriert: alle 285 Docs `unverifiziert` ueber alle drei Stroeme -- ehrlicher Anker. Site-Konsistenz: Footer auf allen 5 Seiten (Edition / Tech-Umsetzung DHCraft / Impressum / GitHub-Icon), `docs/impressum.html` als Stub angelegt (ZBZ + DHCraft + Haftungsausschluss), `Experimentell`-Badge im Header-Zentrum mit Tooltip statt eigener Disclaimer-Zeile. Datenpflege: `_normalize_author` in `generate_edition_data.py` faengt HARTE-Caps (z.B. "JEANNE HERSCH" → "Jeanne Hersch", Threshold 80% Grossbuchstaben). Bonus: **E63 Phase 2 Schritt 3** geschlossen -- `detectBlankPage` im Viewer liest jetzt primaer `<pb type="blank"/>` aus der per-Seiten-TEI; OCR-Heuristik `isBlankPageText` bleibt Fallback fuer Faelle ohne TEI. Backup-Folder `output/_backup_pre_status_marker/` aus Test-Lauf entfernt. Lehre: Status-Werte gewinnen erst dann Aussagekraft, wenn ihre Default-Bedeutung mit der Realitaet uebereinstimmt -- "offen" implizierte "nichts da", die Realitaet war "vorhanden, unverifiziert". Iterative UI-Diskussion mit dem User funktioniert nur, wenn man eigene Vorschlaege ehrlich auf den Tisch legt und Befunde nicht als Geschmack abtut. |
| 50 | 2026-05-26 | Agent-Screening abschaffen, Workflow-Status pro Strom einfuehren (E66). User-Befund: kein Mensch hat die 285 "APPROVED" gegeben — Agent screent sich selbst mit eingebauter Ignorier-Liste (W3/W6/W10 als "normal" deklariert), Etikett gegenueber ZBZ irrefuehrend. Ersatz: vier Statuswerte (offen | in_arbeit | bearbeitet | fertig) je Datenstrom (ocr, layout, tei), gesetzt von Menschen im Viewer. Datenmodell: Pro-Objekt-Manifest aus E65 erweitert — `streams.*` vom statischen Engine-Deskriptor zum Objekt `{engine, status, history}` mit voller Provenienz-History `[{at, by, from, to, note}]`. `page_manifest.py` idempotent gemacht, schreibt jetzt fuer alle 285 Docs (vorher nur die 15 mit Leerseiten). `generate_edition_data.py`: Catalog liest `streams.*.status` aus Manifest statt `screening` aus `_review.json`, Manifeste zu `docs/data/manifests/` gespiegelt, corpus-Histogramm `stream_status`. Catalog-UI ([index.html](../docs/index.html), catalog.js, catalog.css): drei Strom-Bars statt Screening-Balken + Filter `Strom × Status` statt Screening-Dropdown + Workflow-Spalte mit drei Status-Zeilen. Viewer-UI: Status-Pills in der Doc-Subbar (zweite Zeile), Klick zykliert offen→in_arbeit→bearbeitet→fertig, erstes Aktivieren eines Edit-Toggles setzt auto offen→in_arbeit, beforeunload-Schutz, "Manifest ↓"-Download, Initialen einmalig per `localStorage` (`zbz.workflow.by`). Legacy: 286 `_review.json` umbenannt zu `_screening_legacy.json` (gitignored), Screening-Badge im Viewer-Doc-Meta entfernt. ZBZ-Uebergabe: neues `scripts/tei/tei_status_marker.py` projiziert History deterministisch als `<change when= who= status= n={stream}>...</change>` in den `<revisionDesc>` und entfernt dabei Eintraege mit `who` matched `^(agent-screening\|quality-screen\|quality-pass\|claude)`; `who="pipeline"` bleibt. Dry-Run auf 30+110 OK, Test-Schreiben auf 30 (3 Summen + 1 Legacy raus) und sofort aus Backup wiederhergestellt — korpusweite Anwendung bleibt bewusste User-Entscheidung. Stand: 285/285 Docs auf `offen` in allen drei Stroemen — der ehrliche Anker. Lehre: das Manifest ist der natuerliche Wohnort fuer nicht-ableitbare menschliche Entscheidungen (E65-Pattern erweitert); status reist via revisionDesc mit dem Dokument (Standard-TEI). |
| 49 | 2026-05-26 | Leerseiten-Manifest + TEI-Marker (E63 Phase 2, Schritt 1+2; E65). `scripts/edition/page_manifest.py`: Pro-Objekt-`{doc}_manifest.json` (gitignored, nur Ausnahmen; class/source/review/evidence + streams-Header), Detektor = OCR-Regel (identisch `ZBZ.isBlankPageText`) + Blank-Marker UND Docling `num_regions==0` als Gegensignal → reproduziert deterministisch **79 Leerseiten in 15 Docs, alle cross-validiert, 0 Konflikte**. `scripts/tei/tei_blank_marker.py`: projiziert `<pb type="blank"/>` in `tei_final` (Seite = sequenzielle pb-Position, identisch Mirror-Splitter) und leert den Body. Dry-Run deckte auf: das vermeintliche "Seitenzahlen behalten" waren groesstenteils auch Muell — "blank"/"Blank Page" als Text + **LLM-Korrektur-Kommentar** ("die OCR-Ausgabe enthaelt nur Sonderzeichen, nichts zu korrigieren") in der TEI. User-Entscheidung: bestaetigte Leerseite = nur Marker, alles leeren (symbolisch + Woerter + Kommentar + Zahlen). 82 Junk-Elemente entfernt, 0 Residual, **0 Schema-Regression** (Backup vs. editiert). Mirror neu (94 Dateien), Browser-verifiziert (doc 20: Leerseite sauber, echte Seite intakt). Lehre: Validator prueft per Default `tei_unified`, nicht die editierten flachen `tei_final` — echte Verifikation per `validate_tei_file` direkt + Backup-Vergleich. Pre-existing Befund O23: alle `tei_final`-Header tragen schema-invalides `<idno>` im publicationStmt (Drift zu tei_unified). Commits: `be5b25ca` (page_manifest), `1a41f604` (tei_blank_marker), `71e806bf` (Mirror). **Offen: Schritt 3** — Viewer von Heuristik auf Manifest/Marker umstellen. |
| 48 | 2026-05-26 | Viewer-UI verdichtet (E64). Drei User-Befunde am laufenden Viewer (`?doc=310`): (1) OCR-Dropdown bot 5 Engines, aber der ausgelieferte Mirror enthaelt nur Mistral — die Alt-Engines (Gemini A/B, LLM, DeepSeek) zeigen auf `../output/` (gitignored, nicht deployed), auf der Live-Seite tot; Dropdown entfernt, `ocrSource` fest auf `mistral`, Prinzip "Viewer = ausgelieferte Edition = Mistral" (Engine-Vergleich liegt in CER-Benchmark/Methode-Seite). (2) Doc-Subbar + Toolbar fusioniert (planmaessig Etappe 2.10): Seitennav in die Subbar, separate `.toolbar` entfaellt; `.toolbar`/`.toolbar__inner`/`.toolbar__group--right` CSS weg, `.doc-subbar__inner` erbt `--v-toolbar-height`. (3) Edit-Toggles "Bearbeiten"/"Bearbeiten" → "Layout"/"Text" (User waehlte Objekt-Label, Aktiv = anthrazit, kein Label-Wechsel/Icon). In Chrome verifiziert (Konsole sauber, Aktiv-Zustand visuell bestaetigt). Lehre: `getComputedStyle` im isolierten Browser-Tool-Kontext liefert bei nativen `<button>` irrefuehrende Werte (meldete transparent trotz greifender anthrazit-Regel) — Bodenwahrheit ist der Screenshot, nicht die berechnete Messung; Inline-Style-Mutationen greifen dort ebenfalls nicht. Doku: decisions E64, viewer.md (Zweck/Toggle/Datenquellen), index.md, workflow.md Etappe 2.10 → teilweise. |
| 47 | 2026-05-26 | Viewer-Live-Review (Chrome) + Leerseiten-Welle. Alle 4 Seiten live geprueft, E62 committet (`f7c3af06`, unpushed). Dok 20 S.1-10 durchgeklickt → UI-Befunde; Schwerpunkt Leerseiten: zeigten OCR-Muell (`^{}[]`/`.`) + Phantom-Regionen (Gemini-Layout-QA halluziniert bis 21 Boxen, Docling=0). Viewer-Fix gebaut + im Browser verifiziert (`core.js` `isBlankPageText`, `viewer.js` blank-Handling, `viewer.css` `.empty--blank-page`): leere Seiten zeigen "Leerseite — kein Text", keine Phantom-Kaesten; echte Seiten unveraendert. Kontrolle "erwischen wir alle Varianten?" → nein: 79 sichere Leerseiten (trivialer Muell + "Blank Page"-Marker) + ~55 Grauzone (Nur-Bild, OCR-Schleifen, Stempel); Auto-Schleifenerkennung erzeugt Fehlalarme (Zeitungskoepfe/Inhaltsverzeichnisse) → Experten-Review noetig. Architektur entschieden: Pro-Objekt-Manifest `{doc}_manifest.json` (eigene Datei, nur Ausnahmen) als Single Source of Truth fuer Seiten-Fakten; TEI-Marker `<pb type="blank"/>` als Projektion daraus; Viewer liest spaeter Manifest statt Heuristik. CLAUDE.md konkretisiert (neue Sektion "Projektstruktur & Datenfluss": Objekt=Datenstroeme, Source-of-Truth→generierter-Mirror-Regel). E63. Offen: Detektor→Manifest-Skript bauen, Marker projizieren, Viewer auf Manifest umstellen, Daten-Bereinigung. |
| 46 | 2026-05-26 | Methode-Seite `docs/methode.html` als schlanke Nachfolgerin des mit E56 abgeschafften CER-Dashboards: Headline-CER (Mean 4.10%/Median 1.83%, 95%-CIs), Stratifiziert (Layout-Typ + Sprache), Literatur-Vergleich (Crosilla et al. 2025, arXiv:2510.06743 — alle 2025+), Limitations (n=19, Selektionsbias, Stability ungemessen), Werkzeug-Doku. Daten statisch (kein Lazy-Load — JSON nicht eingecheckt). `.prose table` zu `base.css` additiv. Nav "Methode" in allen 3 Hauptseiten; About-Qualitaets-Absatz verlinkt jetzt zur Methode-Seite. E62. Memory: `feedback_no_llm_for_id_linking.md` neu (GND/Wikidata-IDs niemals per LLM — bestaetigt durch entities.md:69-71 "kein LLM fuer IDs", Datei mit E71 entfernt). |
| 45 | 2026-05-25 | Edition-Uplift-Welle gestartet (Plan: `~/.claude/plans/edition-uplift-three-pages.md`). Vier Etappen ueber alle drei Frontend-Seiten (index, viewer, about) + Quer-Politur, ~25 h. Architektur-Entscheidungen: E58 OpenSeadragon 5.0.1 als Faksimile-Renderer (Pan/Zoom, View-Modus), E59 Polygone explizit ausgeschlossen (Druck-Korpus, Rechtecke reichen), E60 Mode-Button-Redesign Option C (Edit-Toggle pro Panel statt globaler Mode-Leiste — loest Wort-Redundanz Transkription↔OCR), E61 Export-Modul mit JSZip 3.10.1 (Per-Doc-Drawer "Alles ↓" + Multi-Select-Bulk-Export aus Korpus-Uebersicht). Code: Schritt 1 OSD-Integration umgesetzt (`viewer.html` CDN-Script, `viewer.css` `.panel__body--canvas`+`.facsimile-osd`, `viewer.js` Renderer zweigeteilt `renderFacsimileOsd()`/`renderFacsimileImg()`, `setMode()` re-rendert bei Variant-Wechsel). Layout-Mode behaelt vorerst Eigenbau-Editor + img. Visual-Test offen. Plan-Erweiterungen on-the-fly: Smart-Filter mit kreuzkonditionalen Live-Counts (1.3), klickbare Spalten-Header (1.4b), Per-Doc-Export-Drawer (2.9), Bulk-Export aus index (1.6). Politur aus Session 44 + Visual-Test-Politur + Befund 1 weiterhin uncommitted (User-Entscheidung). |
| 44 | 2026-05-25 | Befund-Fixes + Konsistenz-Refactoring. Fixes: Frontend-Screening-Zaehler `WITH_NOTES`→`APPROVED_WITH_NOTES` (catalog.js/index.html); TEI-Doppelkodierung `&amp;amp;` behoben (tei_step1.py `html.unescape` + Bestandsdaten 26 Docs). Refactoring: Knowledge-Drift nach E56/E57 bereinigt (viewer.md auf reale 3-Seiten-App, CLAUDE.md cer.html-Verweis entfernt, `data/tei_curated/` angelegt, E56-curation_server-Widerspruch, shared.js→core.js, edition→viewer-Knoten); tote CSS-Tokens entfernt; `.btn--ghost`-Dopplung auf `#filter-reset` gescoped; Orphans geloescht (`scripts/postprocess/`, `generate_dashboard_data.py` + `dashboard.json`). Befund 3 geschlossen: 6 Finals (revisionDesc-Escaping -- Writer `tei_add_revision.py` + Bestand) und 309 Per-Seiten-Fragmente (`<pb>`-Splitter balancierte `<div>` nicht -> `_balance_divs` in `generate_edition_data.py` + Bestand). Alle 4970 served XML strikt wohlgeformt (vorher 327 nicht). |
| 43 | 2026-05-25 | Viewer auf vollen Korpus erweitert: `scripts/edition/generate_edition_data.py` mit `mirror_per_page_data()` (alle 285 Docs → `docs/data/pages/`, 8083 Layout + 4117 OCR + 4115 TEI-Seiten extrahiert via `<pb>`-Splitting, sequentielle Pagination 1..N statt n-Attribut). `core.js`-Resolver mit dreistufigem Fallback (`pages/` → `examples/` → `../output/`). `docs/.nojekyll` fuer GitHub Pages. Deployment-Section in `viewer.md`. Smoke-Test via HTTP-Probes (5 Docs ueber Typen A-D) erfolgreich. Bildlieferung bleibt lokal-only (4 GB). E57. |

---

## April 2026 — Frontend-Radikalkur + Wissenschaftliche CER-Re-Evaluation

| # | Datum | Thema |
|---|---|---|
| 42 | 2026-04-27 | Knowledge-Konsolidierung (25→10 Docs, alle filenames lowercase) + Frontend-Radikalreduktion: Edition + Diagnostik + CER-Dashboard + Curation Editor ersatzlos abgeschafft. Neue Single-Page-App `docs/viewer.html` mit Faksimile + OCR/TEI-Panel + Layout- und Transkriptions-Editor. Persistenz via Datei-Download (kein FastAPI mehr). Volumen-Reduktion: 9→1 HTML, 23→6 JS (−81%), 5.023→806 Z. CSS (−84%). E56. |
| 41 | 2026-04-27 | CER wissenschaftlich fundiert: BCa-Bootstrap-CIs (B=10000, Seed=42), Paired-Test E2E vs OCR-only (−14.83 pp, p<0.001), HCPR ~99%, Selektionsbias n_chars p=0.041 ehrlich geflaggt. Multi-Claude-Koordination ueber CLAUDES-WORKING-SESSION.md. Pagewise-vs-Global-Mess-Artefakt diagnostiziert und in [quality.md](quality.md) dokumentiert. Interaktives Dashboard `docs/infrastruktur/cer.html` mit 12 Sektionen. E54/E55. |

---

## Maerz 2026 — Pipeline-Konsolidierung + Edition

| # | Datum | Thema |
|---|---|---|
| 40 | 2026-03-27 | Frontend-Refactoring Phase 1+2: CSS-Token-Konsolidierung, HTML-Semantik (Skip-Nav, ARIA), JS-Foundation-Layer (`zbz-core.js`), Unified TEI Renderer. |
| 39 | 2026-03-26 | OCR-Diagnostik Abschluss: 6 Scope-Mismatches identifiziert; bereinigte Statistik n=19 Mean 4.18% / Median 1.83%. |
| 38 | 2026-03-26 | Diagnostik-UI Rewrite: 4 Tabs, ZBZ.Diagnostik-Namespace, Search-Index 279→285 (XML-Parsing-Fix). |
| 37 | 2026-03-26 | Diagnostik-Datenproduktion: W10-Tiefenanalyse, Corpus-Statistik (285 Docs / 4.108 Seiten), Validierungs-Timeline. |
| 36 | 2026-03-26 | Edition-Sync Fortsetzung: Log-Tab, Seitenzaehlung 383→4.117. |
| 35 | 2026-03-26 | Edition-Synchronisation: Katalog 15→285 Docs, Wikidata-Resume-Flag, revisionDesc im Reader. |
| 34 | 2026-03-26 | TEI-Qualitaet: ref-Pattern in `zbz_hersch.rng` erweitert (GND + #zbz), 285/285 schema-valide. Heuristische lb-Injection (10.635 lb in 46 Docs), Post-Assembly-Fixes W3/W4/W7. |
| 33 | 2026-03-26 | OCR-Diagnostik + Eval-Optimierung: Symmetrische Normalisierung, Hyphen, CI-Alignment. Mean CER 9.33%→5.97%, Median 5.52%→2.42%. |
| 32 | 2026-03-26 | End-to-End CER Benchmark (E51): TEI-vs-TEI Eval, `benchmark_cer.py`, Median 5.5%. Sub-Projekt CER-Verbesserung definiert. |
| 31 | 2026-03-26 | Neues Schema `zbz_hersch.rng` + verbindliche Editionsrichtlinien ZBZ eingearbeitet (18 Dateien). E48/E49/E50 (Dual-Attribut). |
| 30 | 2026-03-15 | Hersch Design-System: Migration auf Anthrazit+Ziegelrot+EB Garamond+Jost. Zweistufige CSS-Tokens (`--h-*` / `--ed-*`). Hersch-Komponenten (Seuil, Etonnement, Polyphonie). |
| 29 | 2026-03-15 | NEEDS_REVIEW 32→0: 20 neue Entity-Stopwoerter (E45), Strukturfixes, OCR-Dedup `ocr_dedup.py` (E46). Finalstand 242 APPROVED / 43 WITH_NOTES / 0 NEEDS_REVIEW. |
| 28 | 2026-03-15 | Edition Frontend Refactoring: Discovery Hub, Volltextsuche, Galerie, Screening + Curation Workflow getrennt, 5 Curation-States. |
| 27 | 2026-03-15 | Agent-Based Quality Screening Rollout 285/285 (58 Batches, 4 Tiers): 210 APPROVED, 43 WITH_NOTES, 32 NEEDS_REVIEW. revisionDesc-Standard etabliert (E42), `output/tei_final/` als Single Source of Truth (E43). |
| 26 | 2026-03-15 | TEI Validation Quality Gate refactored: 2-Ebenen (Errors/Warnings), W1-W11, HTML-Report. Entity-Tagging typkorrekt mit internen IDs. div-Merge. `--reassemble` Flag. 284/285 VALID. |
| 25 | 2026-03-14 | Frontend-Konsolidierung: Edition nach `docs/`, Pipeline-UI nach `docs/infrastruktur/`. ES5→ES6+ in 13 JS-Dateien. |
| 24 | 2026-03-12 | Viewer-Erweiterung: WD/zbz-ID Support, GND-0%-Bug behoben (`entity_index.py` schrieb GND nie ins TEI-XML — Fix + Cache-Backfill, 0%→21.7%). |
| 23 | 2026-03-09 | NER Completion + TEI Entity Injection: 285 Docs, 11.685 Entities, 26.197 Mentions. Wikidata-Linking gestartet. |
| 22 | 2026-03-09 | Knowledge-Refactoring: EDITION + CURATION getrennt. NER Production Run 285 Docs, 4.100 Index-Eintraege. |
| 19-21 | 2026-03-08–09 | Curation Editor Phasen 2-5: Block-Toolbar, Entity-Kuration mit Autocomplete, Review-Workflow (3 Status), TEI-Validierung. `data/tei_curated/` als git-tracked Gold-Standard. |
| 17-18 | 2026-03-08 | tei_unified Refactoring (Orchestrator ~1100→~70 Z.). NER-Robustheit (Diakritik, Retry, Surname-Matching). NER Production Phase 1 (7 Qualitaetsverbesserungen, E35). |
| 14-16 | 2026-03-06–07 | Unified TEI Pipeline (E32): 4 Stufen (Scaffold + Gemini + Assembly + Validation). NER Pipeline (E34): Gemini Flash Lite, 6 Entity-Typen, Wikidata-Reconciliation. |
| 12-13 | 2026-03-06 | Gemini Vision TEI (E30, superseded). Dokumenttyp-spezifische Prompts (4-Ebenen). Layout-QA Full Run E31 (14.708 Korrekturen). |
| 11 | 2026-03-05 | Gemini-Dokumentklassifikation (E27, Stage 1a). Online-Demo (E28). Gemini OCR-Korrektur Stage 2b (E29). |
| 9-10 | 2026-03-03–04 | docling-serve API (E24), Gemini Layout QA + Detect (E25/E26): 3 Modi (qa/detect/auto). |

---

## Februar 2026 — Pipeline-Aufbau

| # | Datum | Thema |
|---|---|---|
| 7-8 | 2026-02-25–27 | Scope-Expansion (E21): Full Pipeline (OCR + Layout + PAGE-XML + NER/GND + TEI). Pilot 15 Docs, page-by-page Comparison (E16/E18). Data Delivery E23 (286 PDFs, 25 TEI-XMLs). |
| 4-6 | 2026-02-14–20 | Mistral OCR 3 als Production Engine (E6). Azure-Integration. PAGE-XML + METS Export (E13, Schema 2013-07-15). Dashboard-Redesign (E15). |
| 1-3 | 2026-01-29–02-14 | Initiale Quellenanalyse: 286 PDFs, 4 Dokumenttypen (A-D), Sprachverteilung FR 66% / DE 30%. Hybrid Pipeline-Entscheidung (E1): Docling Layout + LLM-OCR Text. |

Aeltere Detail-Eintraege im Git-Verlauf erhalten.

---

## Wiederkehrende Muster

Aus den Sessions destillierte Beobachtungen, die fuer kuenftige Arbeit relevant bleiben:

- **L1** Validierung muss actionable sein. False-Positive-Quote >50% macht Reports nutzlos. Jede Warning braucht eine konkrete Aktion.
- **L2** Entity-Typ darf nicht verloren gehen. `annotate_entities()` braucht `(tag, id)` aus dem Index, nicht nur Namen.
- **L3** Stopwort-Filter ist essenziell. Gattungsbegriffe (Mensch, Gott, Wahl) erzeugen ohne Filter ~30% False Positives.
- **L4** Seiten-Fragmente zu Dokument-Struktur mergen. ZBZ-Referenz hat 1 top-level div. Post-Assembly-Merge ist deterministisch und kostenlos.
- **L5** Step-2-Cache invalidieren bei Prompt-Aenderungen. `--force` regeneriert nicht den Step-2-Cache.
- **L6** LLM-NER hat ~5-10% False Positives. Inhaerent. Loesung: Curation Editor, nicht Code-Fix.
- **L7** Page-Numbering-Drift macht Pagewise-CER unbrauchbar. Content-aligned Eval (`evaluate_tei_vs_tei`) ist immun.
- **L8** Mehrsprachige Codes korrekt parsen. "fra/deu" zerfaellt sonst zu "und". Betrifft ~40 Docs.
- **L9** facsimile/pb synchron halten. Leere surfaces fuer Seiten ohne Layout-Zones.
- **L10** Interne IDs (zbz-p/o/l/w.N) als primaere Referenz. GND in `ref`, intern in `corresp` (Dual-Attribut, E50).
- **P7** Gattungsbegriffe im Entity-Index erzeugen False Positives in ~30% der Docs.
- **P8** Zeitungslayouts versagen systematisch (>40 Zones, OCR-Halluzinationen). ~3% des Korpus.
- **P10** Tier-2-Docs (4-8 Seiten) haben 85%+ APPROVED-Rate, Tier-1 (1-3 Seiten) nur 40%.
