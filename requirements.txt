# ZBZ-OCR-TEI Pipeline
# Python 3.11+

# OCR (GPU erforderlich)
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
accelerate>=0.24.0

# PDF-Verarbeitung
pypdfium2>=4.0.0
Pillow>=10.0.0
PyMuPDF>=1.23.0           # PDF-Splitting fuer Mistral (max 30 Seiten/Request)

# API-Zugang (Mistral Document AI auf Azure)
requests>=2.31.0

# TEI/XML
lxml>=4.9.0

# Evaluation
jiwer>=3.0.0          # CER/WER Berechnung
rapidfuzz>=3.0.0      # String-Matching

# Optional: Docling (Alternative OCR)
# pip install docling "docling[easyocr]"
# Hinweis: Erfordert Windows Developer Mode (Symlinks)

# LLM Post-Correction (optional, Stage 2a)
anthropic>=0.39.0

# Gemini API (Layout QA, Classification, OCR Correction)
google-genai>=1.0.0

# Entwicklung
pytest>=7.0.0
