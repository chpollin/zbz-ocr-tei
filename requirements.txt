# ZBZ-OCR-TEI Pipeline
# Python 3.11+

# config (.env -> env vars; top-level import in scripts/config.py)
python-dotenv>=1.0.0

# PDF processing
pypdfium2>=4.0.0
Pillow>=10.0.0
PyMuPDF>=1.23.0           # PDF splitting for Mistral (max 30 pages/request)

# HTTP (OCR APIs, Transkribus upload)
requests>=2.31.0

# TEI/XML
lxml>=4.9.0

# evaluation
rapidfuzz>=3.0.0          # Levenshtein opcodes (CER decomposition)
numpy>=1.26.0             # CER statistics (bootstrap)
scipy>=1.11.0             # CER statistics (tests/distributions)
pyspellchecker>=0.7.0     # quality proxy (dictionary hit rate)
openpyxl>=3.1            # Masterfile.xlsx read in scripts/eval/corpus_audit.py

# LLM post-correction (optional, stage 2a; lazy import)
anthropic>=0.39.0

# Gemini API (layout QA, classification, OCR correction, TEI step 2)
google-genai>=1.0.0

# optional: torch (lazy import in scripts/utils.py, local GPU experiments only)
# pip install torch

# optional: Docling layout engine (requires Windows Developer Mode for symlinks)
# pip install docling "docling[easyocr]"

# development
pytest>=7.0.0
